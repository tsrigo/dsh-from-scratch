[project]
name = "dsh-concept-films"
version = "0.1.0"
description = "Manim explainers for the DeepSeek Harness from Scratch tutorial"
requires-python = ">=3.12,<3.13"
dependencies = [
  "manim==0.20.1",
]

[tool.uv]
package = false

